{{!< default}}

<div class="row">
  <div class="col-md-12">
    <div class="page-header" id="blog-header">
      <h1>{{@blog.title}} <small>{{@blog.description}}</small></h1>
      {{#if @blog.cover}}
        
      {{/if}}
    </div>
  </div>
</div>

{{> "loop"}}

{{pagination}}
