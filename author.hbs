{{!< default}}

{{#author}}
  <div class="row">
    <div class="col-md-12">
      {{#if cover}}
        <div class="page-headerish" style="background-image: url('{{cover absolute="true"}}')">
      {{else}}
        <div class="page-headerish">
      {{/if}}
          <div class="page-header-inner">
            <h1>{{name}}</h1>
            <h2>{{bio}}</h2>
          </div>
        </div>
    </div>
  </div>
{{/author}}

{{> "loop"}}

{{pagination}}
