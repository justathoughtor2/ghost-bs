{{!< default}}

{{#author}}
  <div class="row">
    <div class="col-md-12">
      <div class="page-header">
        <div class="page-header-inner">
          <h1>{{name}}</h1>
          <h2>{{bio}}</h2>
        </div>
        {{#if cover}}
          {{blog-header-bg cover}}
        {{/if}}
      </div>
    </div>
  </div>
{{/author}}

{{> "loop"}}

{{pagination}}
