{{!< default}}

{{#author}}
  <div class="row">
    <div class="col-md-12">
      {{#if cover}}
        <div class="page-header" style="background-image: {{cover absolute="true"}}">
      {{else}}
        <div class="page-header">
      {{/if}}
          <div class="page-header-inner">
            <h1>{{name}}</h1>
            <h2>{{bio}}</h2>
          </div>
        </div>
    </div>
  </div>
{{/author}}

{{> "loop"}}

{{pagination}}
